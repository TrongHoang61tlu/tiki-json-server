{
  "name": "tiki-json-server",
  "version": "1.0.0",
  "description": "tiki json-server",
  "main": "nain.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon main.js",
    "start": "npm run generate-data  && node main.js",
    "generate-data": "node generate-data.js"
  },
  "keywords": [
    "json-server"
  ],
  "author": "Hoang",
  "license": "MIT",
  "dependencies": {
    "casual": "^1.6.2",
    "json-server": "^0.17.1"
  },
  "devDependencies": {
    "faker": "^5.5.3",
    "nodemon": "^2.0.20"
  }
}
